steps:
- name: 'gcr.io/cloud-builders/docker'
  entrypoint: 'bash'
  args: ['-c', 'docker pull us-docker.pkg.dev/appintegration-toolkit/images/integrationcli:latest', 'integrationcli integrations list --verbose']
# - name: 'gcr.io/cloud-builders/gcloud'
#   entrypoint: 'bash'
#   args: ['test.sh']
# - name: 'bash'
#   script: |
#     #!/usr/bin/env bash
#     integrationcli integrations list --verbose
options:
  defaultLogsBucketBehavior: REGIONAL_USER_OWNED_BUCKET
